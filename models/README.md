# Local model assets

No model is selected or bundled yet. Future assets must include source/license, SHA-256, input/output contract and held-out benchmark evidence. Never download a model at runtime.
